# Generated by scripts/generate-pack-manifests.ts from the runtime descriptor.
# Regenerate rather than hand-editing: manifestDrift compares this file to the
# descriptor in index.ts, and the descriptor is what the registry resolves.

id: research-pharma
version: 1.0.0
entry: index.ts
extends:
  - id: healthcare-core
    version_range: '^0.2.0'
applies_to:
  organization_kinds: [provider, payer, health-system, practice, pharmacy]
capabilities:
  - clinicaltrials-gov
  - dailymed
  - openfda
  - faers
  - pubmed
  - rxnav
  - trial-matching
  - new-drug-surveillance
  - pharmacovigilance-signal-detection
cms_universe: []
required_controls:
  - audit-provenance
  - attribution-nlm
  - attribution-fda
