# Generated by scripts/generate-pack-manifests.ts from the runtime descriptor.
# Regenerate rather than hand-editing: manifestDrift compares this file to the
# descriptor in index.ts, and the descriptor is what the registry resolves.

id: primary-care-deep
version: 1.0.0
entry: index.ts
extends:
  - id: healthcare-core
    version_range: '^0.2.0'
applies_to:
  organization_kinds: [practice, health-system, provider]
  facility_kinds: [primary-care]
capabilities:
  - awv
  - uspstf-preventive
  - chronic-disease-programs
  - behavioral-health-screening
  - ccm-tcm-billing
  - pediatric-well-child
  - sdoh-referral
cms_universe: []
required_controls:
  - audit-provenance
  - phi-handling
  - access-policy
