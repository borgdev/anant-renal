# Generated by scripts/generate-pack-manifests.ts from the runtime descriptor.
# Regenerate rather than hand-editing: manifestDrift compares this file to the
# descriptor in index.ts, and the descriptor is what the registry resolves.

id: infusion-provider
version: 0.1.0
entry: index.ts
extends:
  - id: healthcare-core
    version_range: '^0.2.0'
applies_to:
  organization_kinds: [provider]
  facility_kinds: [infusion-center, home-infusion]
capabilities:
  - chair-scheduling
  - drug-preparation
  - infusion-administration
  - reaction-monitoring
  - prior-auth-coordination
cms_universe:
  - id: cms:mips
    title: MIPS
    authority: CMS
required_controls:
  - access-policy
  - audit-provenance
  - data-quality
  - reaction-alerting
