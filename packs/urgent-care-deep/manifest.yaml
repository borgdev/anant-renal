# Generated by scripts/generate-pack-manifests.ts from the runtime descriptor.
# Regenerate rather than hand-editing: manifestDrift compares this file to the
# descriptor in index.ts, and the descriptor is what the registry resolves.

id: urgent-care-deep
version: 1.0.0
entry: index.ts
extends:
  - id: healthcare-core
    version_range: '^0.2.0'
applies_to:
  organization_kinds: [provider, practice, health-system]
  facility_kinds: [urgent-care]
capabilities:
  - walkin-registration
  - esi-triage
  - clinical-decision-rules
  - poc-testing
  - emtala-transfers
  - occupational-health
  - nsa-good-faith-estimate
cms_universe: []
required_controls:
  - audit-provenance
  - phi-handling
  - access-policy
  - emtala-compliance
